# ATPM 模型逻辑审查报告

## 审查边界与结论

本次只检查并注释代码，没有更换项目的形状、OVIRS、SPICE、热物性、粗糙度、
光谱响应或反演输入。审查所依据的理论主线是 Rozitis & Green (2011) 公开的
ATPM 公式和用户提供的带注释参考程序。

结论：当前 Bennu 正演中没有发现会因“缺少 90° 半球坑、局部坐标映射、ACF
或式(24)”而把论文热惯量 350 系统性推到当前约 184 的逻辑错误。原代码已经
实现这些步骤，只是 ACF 被代数合并进 `2/N` 权重，容易被误认为遗漏。本次把
ACF 显式化并增加守恒测试，所得权重与修改前逐元素完全相同，因此没有改变任何
正演数值。现有热惯量差异仍主要属于输入产品和近似层级不同，而不是这四处坑模型
逻辑错误。

## 与论文 ATPM 建模步骤的逐项对应

| 论文/参考代码步骤 | 本项目位置 | 审查结果 |
|---|---|---|
| 宏观三角面元、外法向和面积 | `bennu_atpm.py::_mesh_from_arrays` | 正确；会纠正朝内绕序并剔除退化面元 |
| 逐帧太阳—面元—观测者几何 | `spice_geometry.py::frame_geometry` | 正确；SPK/CK/IK 结果转到 `IAU_BENNU`，使用 `LT+S` |
| 宏观面元到粗糙坑局部坐标 | `_facet_bases`、`_directions_in_facet_frame` | 正确；局部基为右手正交基，已加测试 |
| 90° spherical-section crater | `_hemispherical_crater_geometry` | 正确；单位半球的 128 单元等面积中点求积，而不是显式三角网格 |
| 坑口阴影与观测可见性 | `_crater_aperture_visibility` | 正确；射线穿过 `z=0` 圆形坑口才可见 |
| 坑壁视因子 | `_hemispherical_crater_geometry` | 正确；式(15)在半球上化为 `A_j/(4πR²)`，对角自视因子为 0 |
| 太阳光多次散射 | `_multiple_scattered_solar` | 正确；直接解 `(I-AF)⁻¹`，等价于迭代至收敛 |
| 坑壁长波自加热 | `_crater_temperatures` | 已实现；热辐射交换与一维导热交替迭代 |
| 周期稳态一维导热 | `_periodic_temperature` | 物理边值问题一致；项目用 Fourier 半无限介质解，而参考程序用深度/时间有限差分 |
| Planck 光谱与 OVIRS 通道卷积 | `planck_lambda`、`planck_ovirs_response` | 已实现；当前公开响应只能近似作者实际处理链 |
| 定向热辐射/thermal beaming | `simulate` 的坑壁观测投影与坑口可见性 | 已实现 |
| 光滑/粗糙端元式(24)混合 | `ModelCurves.mixed`、`mixed_detector` | 正确；`(1-f_R)F_s+f_RF_r` |
| 全局形状遮挡/自加热 | `shape_radiation.py` | 已实现稀疏面元视因子与 Embree 遮挡；扫描时的抽样网格仍是近似 |

## 为什么 `2/N` 已经包含 ACF

令单位半球分成 `N` 个等面积坑壁单元，则每个真实球面面积为

```
dA = 2π/N
```

论文附录中的面积转换因子为“宏观面元面积 / 坑壁在局部水平面上的总投影
面积”。把宏观面元面积归一为 1 后，单位半球的投影面积为

```
Σ dA n_z = π
ACF = 1/π
```

所以式(24)粗糙面元积分实际使用的权重为

```
ACF × dA = (1/π)(2π/N) = 2/N
```

原代码直接写 `weights = 2/N` 在数学上是正确的。本次改为分别保存
`raw_patch_areas`、`projected_area`、`area_conversion_factor` 和
`area_weights`，只是把同一个关系写清楚。128 单元的实测审计量为：

- 坑口投影面积：`π`
- ACF：`1/π = 0.3183098861837907`
- 与原 `2/N` 权重的最大差：`0.0`
- ACF 后坑壁面积/宏观面元面积：`2.0`
- ACF 后水平投影面积/宏观面元面积：`1.0`
- 单行坑壁视因子和：`0.49609375`，即理想 `0.5` 减去离散单元的自项

因此若再在盘积分处额外乘一次 ACF，会错误地把粗糙端元压低到原来的约 31.8%。

## 等面积求积和显式三角坑网格的关系

用户提供的 V 型槽程序用四个显式三角面元验证通用求解链。本项目的标准坑采用
连续半球上的等面积配点求积：每个配点代表一个有限面积壁元，所以同样能进入
视因子、散射、自加热、阴影和定向积分。对于规则 90° 半球，解析几何核使这种
写法比人为三角剖分更直接，也没有额外网格接缝。

它不是无限分辨率：坑口阴影边缘和可见边缘在单元内仍按配点的 0/1 状态处理。
提高 `crater_theta_bins` 和 `crater_azimuth_bins` 可以检查该离散误差，但把同一
半球改写成三角片本身不会自动消除误差；决定精度的是单元尺度与收敛性。
项目已有收敛表显示，相对 200 单元参考曲线，当前 128 单元在 4 µm 的 RMS 差为
`0.741%`、最大差为 `1.572%`；而早期 8 单元分别达到 `8.010%` 和 `22.384%`。
所以把生产配置从 128 再提高到 200 有价值，但其已测影响远小于当前反演的系统残差。

## 为什么热惯量仍与论文不同

当前结果约 `Γ=184 J m⁻² K⁻¹ s⁻¹/²`，论文为 `350±20`。本次审查不重新运行
全 Γ 网格，因为 ACF 重构的数值差为零。现有 `reduced χ²≈433` 已直接说明模型
和数据处理链之间存在远大于观测形式误差的系统差异，不能把 184 当成论文结果的
独立精确复现。影响优先级如下：

1. 论文使用作者内部 L3a 处理序列；公开归档只有 L2 v2，作者的精确欠填充 FOV/
   孔径经验校正数组没有公开。
2. 论文拟合 3.5–4.0 µm 的旋转分辨光谱；当前 Γ 扫描主要比较补充图 2b 的
   3.98–4.02 µm 单条光变，约束并不等价。
3. 当前使用公开 SPCv14 12.56 m 形状，而论文报告的是 SPCv13。
4. 当前保存结果的 Γ 扫描只取 12288 个宏观面元中的 96 个（stride=128）。抽样
   面积可以保持平均通量，却不能保持完整非凸遮挡和面元互热几何。
5. OVIRS 通道线形和孔径响应来自公开 IK/近似径向函数，不等于作者 L3a 使用的
   全部仪器标定与经验修正。
6. SPICE 几何在逐帧计算后被分箱并插值到有限热模型相位网格；这仍是时间离散近似。
7. 参考 ATPM 允许宏观形状面元的太阳多次散射及光滑/粗糙端元与全局互热完全
   耦合；当前程序精确处理坑内交换，但全局互热场先由光滑端元求出，再作为同一
   外部场加入坑壁。这是低反照率、近凸 Bennu 下的近似，不是完整耦合求解。

上述差异足以改变光变振幅、相位滞后和绝对标度，而热惯量正是通过这些特征拟合的。
因此，下一步若目标是缩小 184 与 350 的差，应优先做“完整 3.5–4.0 µm 联合拟合、
更密的宏观面元 Γ 扫描、取得作者 L3a/SPCv13/孔径修正”，不是在正确的 ACF 上再
增加一个倍率。

## 自动检查

`test_bennu_atpm.py` 现包含以下专门测试：

- 半球坑投影面积与 ACF 守恒；
- 坑壁总面积、视因子和及多次散射；
- 宏观面元—粗糙面元局部基的正交性和手性；
- 式(24)在 `f_R=0`、`0.5`、`1` 的端点和线性混合；
- Planck 单调性、OVIRS 卷积、正演残差与形状面积。

本次执行 `test_bennu_atpm.py` 与 `test_pds_observations.py` 共 10 项，全部通过。

## 理论来源

- Rozitis & Green (2011), *Directional characteristics of thermal-infrared
  beaming from atmosphereless planetary surfaces – a new thermophysical model*：
  https://academic.oup.com/mnras/article/415/3/2042/1044431
- 作者接受稿归档页：https://oro.open.ac.uk/30600/
