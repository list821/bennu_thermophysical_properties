# Bennu 论文模型输入数据与下载说明

本文所指论文是 DellaGiustina et al. (2019), *Properties of rubble-pile
asteroid (101955) Bennu from OSIRIS-REx imaging and thermal analysis*。论文中的
“数据可获得”不等于“论文图表所用的整理后数组已作为一个附件发布”：原始/标定产品在
NASA PDS，精确的作图支持数据需按论文 Data availability 向通讯作者索取。

## 1. 热物理模型 ATPM

### 实际观测输入

| 输入 | 文件中的形式与字段 | 本文采用范围 | 来源 |
|---|---|---|---|
| OVIRS 光谱 | 每次约 1 s 曝光一个 FITS；辐亮度随波长的数组，单位 W cm⁻² sr⁻¹ µm⁻¹；附误差/噪声、质量标志、暗场、波长以及 `MIDOBS`、`PHASEANG`、`FILL_FAC`、`SUN_RNG`、`TRGTRNG` 等头信息 | 2018-11-02、03，每天约 4.35 h；论文取 3.5–4.0 µm，先减去按约 2.1 µm 缩放的太阳反射分量，再按 1° 自转相位分箱 | NASA PDS Small Bodies Node，`orex.ovirs/data_calibrated/approach` |
| OTES 光谱 | 每天一个二进制 `.dat` 加 PDS4 `.xml` 标签；逐记录含 349 点 `xaxis` 波数和 `cal_rad` 标定辐亮度、质量标志、最大亮温及不确定度 | 2018-11-08、09，每天约 4.49 h；论文使用约 14 µm（约 714.3 cm⁻¹）的热辐射 | NASA PDS Small Bodies Node，`orex.otes/data_calibrated/approach` |
| 每次曝光的几何 | OTES `_ote_geo.fits/.xml`，以及 OVIRS FITS 头和 SPICE；包括太阳、目标、航天器位置姿态，日心/观测距离、相位角、视场填充率 | 对每个观测时刻计算各三角面元的入射角、出射角、阴影及可见性 | OTES geometry 集合和 OSIRIS-REx SPICE archive |

论文中报告的观测条件包括：OVIRS 11 月 2/3 日约 197/190 km、日心距
1.041/1.037 au、相位角 5.1°/4.5°；OTES 11 月 8/9 日约 162/159 km、
日心距 1.021/1.018 au、相位角 4.5°/5.7°。这些数字用于核对下载文件，不能
代替逐曝光几何。

### 物理和形状输入

| 输入 | 论文设置 | 从哪里来 |
|---|---|---|
| 三维形状网格 | SPC v13，热计算约 12 m 面元 | OCAMS 影像立体测量/SPC；历史 v13 未发现与论文一起直接公开。脚本下载公开的 v21、12.6 m DSK，分辨率相近但并非完全相同版本 |
| 自转状态 | 周期 4.296061 ± 0.000002 h；极轴赤经 85.3° ± 0.2°、赤纬 −60.2° ± 0.2° | OCAMS 影像/SPC 解及 SPICE PCK/CK |
| Bond 反照率 | 0.016 | 论文的 OCAMS 光度分析；作为 ATPM 固定参数 |
| 发射率 | 0.9（辐射/光谱发射率假设） | 论文固定假设，不是单独数据文件 |
| 热惯量网格 | Γ = 0–600 J m⁻² K⁻¹ s⁻½，步长 10 | 待拟合的模型参数，不是下载数据 |
| 表面粗糙度 | 半球形坑覆盖率 0–1；包含多次散射、自阴影和自加热 | 待拟合的 ATPM 参数化，不是下载数据 |
| 自转相位 | 与热惯量、粗糙度共同最小化 χ² | 待拟合参数 |

论文得到的单组 ATPM 主要结果是 Γ = 350 ± 20 J m⁻² K⁻¹ s⁻½、粗糙度分数
0.77 ± 0.04。100 次 bootstrap 同时传播单点和绝对标定误差。

## 2. 双组分热模型

该模型复用上面的 OVIRS/OTES 光谱、形状、自转、几何、反照率和发射率，但把
表面分成细颗粒区和巨石区。额外输入来自 OCAMS 影像统计的巨石面积/空间覆盖率；
细颗粒热惯量以单组分解为约束，改变巨石热惯量并把两类面元的辐射按面积组合。
因此它并不存在一个独立的“双组分传感器文件”：额外数据是从影像派生的巨石覆盖率。

## 3. 光度模型

论文采用 Lommel–Seeliger 盘函数乘指数相函数。输入为 Approach 和 Preliminary
Survey 阶段的 MapCam L2/L3 标定影像：像素 I/F、滤光片、曝光、相位角，以及由
ISIS3 + SPICE + v13 形状模型得到的每像素入射角、出射角、方位和阴影。拟合输出
包括几何反照率、相位函数/相位积分和 Bond 反照率。OCAMS 全 Approach 标定集合约
95 GB，不是复现 ATPM 所必需，故默认下载器不自动拉取。

## 4. 巨石尺寸—频数模型

输入是 2018-12-01 的 PolyCam 未投影 L2 影像（另以 12 月 16 日 MapCam 拼图辅助），
以及人工/半人工描绘的巨石轮廓。每条记录应至少包含影像 ID、轮廓坐标、投影长度、
面积、像元尺度和位置；模型对累计数量—直径关系拟合幂律，用 Poisson 误差，并用
KS 检验确定完备直径。精确的论文统计表/轮廓集合需要从 PDS 高级产品中匹配或向作者
索取，不能从热光谱文件推回。

## 5. 热惯量到颗粒尺寸/趋肤深度模型

这不是新的遥感数据集。观测输入是 ATPM 得到的 Γ；材料输入是文献/假设范围，例如
比热约 500–1100 J kg⁻¹ K⁻¹、颗粒密度约 2000–2900 kg m⁻³、孔隙率约 0.1–0.9，
再通过接触传导加孔隙辐射传导模型换算颗粒直径和热趋肤深度。论文给出的量级为毫米到
数厘米颗粒，趋肤深度约 0.8–3 cm。此类常数应记录在配置文件中，而不是在 PDS 下载。

## 下载方法

在项目目录运行：

```powershell
python download_paper_inputs.py
```

默认会下载并校验：论文 PDF、OVIRS/OTES 数据说明、两天 OTES 标定数据、两天全部
OTES 几何、每天一个 OVIRS 样例、公开 12.6 m Bennu DSK，以及覆盖这些日期的核心
SPICE kernel。每个文件的原始 URL、字节数和 SHA-256 写入
`data/download_manifest.json`。重复运行会跳过已有文件。

每天一个 OVIRS 样例只能说明数据“长什么样”，不能完成论文拟合。完整拟合需要
2018-11-02 和 03 的 34,254 个 science FITS 及对应 XML 标签，约 19 GB、共约
68,508 个文件。确认磁盘空间后运行：

```powershell
python download_paper_inputs.py --full-ovirs --confirm-large-download
```

下载器会先读取 PDS 目录索引，并验证恰好发现 34,254 个 FITS 后才下载，避免静默取得
错误日期或产品级别。

## 哪些内容仍需向作者索取

要做到逐点复现论文图和完全相同的 χ²，仍需：历史 SPC v13 网格、论文实际使用的
OVIRS L3a 整理后光谱/质量筛选、反射光扣除后的 1° 分箱数组、OTES 对应整理数组、
绝对标定误差实现、巨石轮廓/统计表及论文定制 ATPM 源码。论文 Data availability
明确说明支撑图表的数据可向通讯作者合理索取；PDS 提供的是可重新处理的任务数据，
两者不是同一个层级。
